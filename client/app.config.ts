export default defineAppConfig({
    nuxtIcon: {
      size: '1.2rem', // default <Icon> size applied
      class: 'icon mx-1', // default <Icon> class applied
      aliases: {
        'nuxt': 'logos:nuxt-icon',
      }
    }
  })